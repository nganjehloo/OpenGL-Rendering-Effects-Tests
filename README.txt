Shadow mapping with PCF, environment mapping through the use of cube maps, normal mapping,
rim lighting, bumpmapping, specular mapping, texture mapping, and irradiance based lighting.

the controls for the application are much like your typical video game. WASD is movement and mouse is view.

The application was built in debug mode so if it runs poorly it is because of that. Given the scene complexity it shouldn't be an issue however.